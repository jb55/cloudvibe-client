
SET(SRCS main.cc frame_division.h enumerated.h)

ADD_DEFINITIONS(-DUSE_SNDFILE -DUSE_FFTW3)

ADD_EXECUTABLE(chords
  ${SRCS}
)

TARGET_LINK_LIBRARIES(chords
  ${CLAM_LIBS}
)

INCLUDE_DIRECTORIES(
  ${CLAM_INCLUDE_DIR}
)

